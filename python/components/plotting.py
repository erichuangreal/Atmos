def add_plot_data(temp_plot, hum_plot, short_timestamp_plot, temp, hum, short_timestamp):
    temp_plot.append(float(temp))
    hum_plot.append(float(hum))
    short_timestamp_plot.append(short_timestamp)
